# AUR packaging

`npm run release:aur:update` generates `dist/aur/PKGBUILD` and `dist/aur/.SRCINFO` from the exact signed AppImage in `dist/`. Generated AUR metadata is release output and is not committed to the application source tree.

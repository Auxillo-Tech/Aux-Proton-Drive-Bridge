# Flatpak status

No Flatpak manifest is shipped. The application depends on the external Proton Drive CLI, and there is no authoritative immutable CLI source or verified offline sandbox build path for the current CLI. The previous draft referenced an unrelated asset and was removed rather than shipping an unsafe package definition.
